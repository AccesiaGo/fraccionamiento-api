# [TÍTULO DE LA TAREA O PR]

## Descripción
Proporciona un resumen claro de los cambios realizados en esta entrega.

---

## Cambios Realizados
- [x] **Módulo:** `fraccionamientos`
- [x] **Características:**
  - Estructura refactorizada (`slug`, `nombre`, `estado`, `ciudad`, `direccion`, `coordenadas`, `telefono`, `correo`, `estatus`).
  - Implementación de **Soft Delete** (`deletedAt` y marca de `inactivo`).
  - Base de datos vinculada a `accesia` en MongoDB Atlas.
  - Integración modular de **Swagger UI** (`/docs`).

---

## Pruebas Manuales y Evidencia

### 1. Documentación Swagger UI (`/docs`)
*Adjunta la captura de pantalla de Swagger UI.*

### 2. Respuesta HTTP (POST / GET)
*Adjunta la captura del payload y la respuesta.*

### 3. Registro en MongoDB Atlas (`accesia.fraccionamientos`)
*Adjunta la captura de MongoDB Compass / Atlas.*

---

## Checklist
- [x] Compilación limpia (`npm run build`).
- [x] Documentación Swagger actualizada.
